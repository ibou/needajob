{% extends "base.html.twig" %}

{% block body %}
    {{ form_start(form) }}
    {{ form_rest(form) }}
     <button type="submit" class="btn btn-primary">S'inscrire</button>
    {{ form_end(form) }}
{% endblock %}